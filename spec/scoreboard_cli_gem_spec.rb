require "spec_helper"

describe ScoreboardCliGem do
  it "has a version number" do
    expect(ScoreboardCliGem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
